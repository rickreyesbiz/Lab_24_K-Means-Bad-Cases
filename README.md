# Lab_24_K-Means-Bad-Cases
This lab examines K‑Means clustering—an unsupervised algorithm that partitions data into groups by assigning points to the nearest centroid—using synthetic scikit‑learn data that intentionally violates K‑Means’ assumption that centroids accurately represent each cluster, highlighting its limitations through analysis and Python coding.
